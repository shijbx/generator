package ${package.Controller};

import com.baomidou.mybatisplus.core.metadata.IPage;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import org.springframework.web.bind.annotation.*;
import ${package.Entity}.bo.${entity}BO;
import ${package.Entity}.vo.${entity}VO;
import ${package.Service}.${table.serviceName};
import us.aiya.reservation.common.domain.DelBO;
import us.aiya.reservation.utils.R;

import javax.annotation.Resource;
import java.util.List;
import java.util.stream.Collectors;

/**
 * <p>
 * $!{table.comment} 前端控制器
 * </p>
 *
 * @author ${author}
 * @since ${date}
 */
@Api(tags = "$!{table.comment}", description = "$!{table.comment}相关接口")
@RestController
@RequestMapping("${entity}")
public class ${table.controllerName} {

    @Resource
    private ${table.serviceName} service;

    @ApiOperation(value = "分页查询", notes = "分页查询")
    @PostMapping("/queryPageByList")
    public R<IPage<${entity}VO>> queryPageByList(@RequestBody ${entity}BO bo) {
        return R.ok(service.queryPageByList(bo));
    }

    @ApiOperation(value = "查询列表", notes = "查询列表")
    @GetMapping("/queryList")
    public R<List<${entity}VO>> queryList(${entity}BO bo) {
        return R.ok(service.queryList(bo));
    }


    @ApiOperation(value = "查询详情", notes = "查询详情")
    @GetMapping("/getInfo/{id}")
    public R<${entity}VO> getInfo(@PathVariable Integer id) {
        return R.ok(service.getById(id));
    }

    @ApiOperation(value = "新增", notes = "新增")
    @PostMapping("/save")
    public R save(@RequestBody ${entity}BO bo) {
        service.save(bo);
        return R.ok();
    }

    @ApiOperation(value = "修改", notes = "修改")
    @PostMapping("/update")
    public R update(@RequestBody ${entity}BO bo) {
        service.updateById(bo);
        return R.ok();
    }

    @ApiOperation(value = "删除", notes = "删除")
    @PostMapping("/delete")
    public R delete(@RequestBody DelBO bo) {
        service.removeById(bo.getId());
        return R.ok();
    }

    @ApiOperation(value = "批量删除", notes = "批量删除")
    @PostMapping("/deleteByIds")
    public R deleteByIds(@RequestBody DelBO bo) {
        service.deleteByBatchIds(bo.getIds());
        return R.ok();
    }
}
