package ${package.Mapper};

import com.bbbus.common.mybatis.core.BaseCURDMapper;
import com.bbbus.dao.operator.domain.${entity};
import org.apache.ibatis.annotations.Mapper;

/**
 * $!{table.comment} Mapper 接口
 *
 * @author ${author}
 * @since ${date}
 */
@Mapper
public interface ${table.mapperName} extends BaseCURDMapper<${entity}> {

}